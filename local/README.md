# Machine-local files

This directory is ignored except for this file. Store teacher engine binaries and evaluations
under `local/teacher/`, plus any other machine-specific paths, caches, or non-secret developer
state needed by local workflows.

Never commit credentials, teacher binaries, or proprietary data.
