# Training models

Model architecture, training objectives, and export code will be introduced after audited data
and teacher labels exist. Initial weights must start from random initialization; third-party
weights are not permitted.
