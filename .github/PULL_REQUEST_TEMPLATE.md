## Summary / 概要

## Verification / 検証

```sh
make check
```

- [ ] `make check` passes (or state exactly what was run instead)
- [ ] Tests cover the change and its failure paths
- [ ] Docs/references updated where behavior changed
- [ ] No datasets, weights, teacher assets, secrets or machine-specific paths added

Strength-affecting change? State the comparison conditions (opponents, clocks,
game counts). 棋力に影響する変更は比較条件を明記してください。
